# 📋 ПАСПОРТ FinLabPy — КОМПАКТНАЯ ВЕРСИЯ

## 🔗 ДОСТУПЫ
- **Локально:** `E:\Python\FinLabProject` (Python 3.14, venv)
- **Сервер:** `root@159.194.219.117` `/root/finlab` (Python 3.14, venv)
- **VS Code Server:** http://159.194.219.117:8080 (пароль: `8a0b7a0073c96143c4057cef`)
- **Токены (в .env):** `MOEX_TOKEN` (Algopack), `TINVEST_TOKEN`, `ALOR_TOKEN`

## 🚨 GIT WORKFLOW
- Код пишем **только локально** → `git push myrepo master --force`
- Сервер: **только pull** (авто при входе)
- ❌ ЗАПРЕЩЕНО: редактировать файлы на сервере
- 🔧 Рассинхрон: `git fetch origin master && git reset --hard origin/master`

## 📦 API MOEXPy

### Фьючерсы:
```python
api.get_candles('RFUD', ticker, dt_from, dt_till, tf)
api.get_futoi(ticker, dt_from, dt_till)  # БЕЗ board!
api.get_candles('TQBR', ticker, dt_from, dt_till, 'D')  # отдаёт M10!
api.get_hi2('stocks', ticker, date)  # engine: 'stocks'/'futures'/'currency'
raw['candles']['data']    # [open, close, high, low, value, volume, begin, end]
raw['futoi']['data']      # [..., clgroup, pos_long, pos_short, ...]
raw['data']['data']       # HI2: [tradedate, metric, value, ...]
FinLabPy/
├── DataCollectors/
│   ├── futoi_collector.py          # Сбор FutOI (cron каждый час)
│   ├── futoi_daily_aggregator.py   # Агрегация D1 (cron 20:00 МСК)
│   ├── hi2_collector.py           # Сбор HI2 (cron 21:00 МСК)
│   ├── hi2_daily_aggregator.py    # Агрегация HI2 (cron 21:05 МСК)
│   ├── m10_to_d1_converter.py     # M10 → D1 конвертер
│   └── osengine_loader.py         # Загрузчик OsEngine
├── My_Indicators/
│   ├── futoi_indicator.py         # FutOI индикатор
│   ├── technical_features.py      # 10 техпризнаков
│   └── hi2_indicator.py           # HI2 загрузчик
├── Strategies/
│   ├── futoi_strategy_GLDRUBF.py  # Золото D1 +63.8%
│   ├── futoi_portfolio.py         # Портфель на FutOI
│   ├── futoi_demo.py              # Демо FutOI
│   └── hmm_strategy.py            # HMM стратегия
└── Docs/
    └── *.md                        # Документация

Теперь отчёт за 07.05 (из паспорта):

```powershell
@'
# 📋 ДНЕВНИК РАБОТЫ — 07.05.2026

## ✅ ЧТО СДЕЛАНО

### 1. Проверка сбора данных FutOI
- Сборщик работает ✅ (каждый час в будни)
- Агрегатор работает ✅ (ежедневно 20:00 МСК)
- GitHub пуш работает ✅ (ежедневно 20:30 МСК)
- **Диапазон данных:** 28.04 — 07.05.2026 (10 дней, 5 тикеров)
- **Статус ML:** отложен до 60+ дней истории (конец июня 2026)

### 2. Исследование API для акций
- **Super Candles:** ❌ API MOEXPy не поддерживает
- **Orderbook:** ❌ только real-time, нет истории
- **Trades:** ⚠️ без фильтрации по датам
- **HI2:** ⚠️ только последняя дата (02.04.2026)
- **Candles M10:** ✅ 7500+ свечей за 90 дней

### 3. Создан конвертер M10→D1
- Загружает 10-минутные свечи, агрегирует в дневные OHLCV
- **Проверен на SBER:** 85 дней с 06.02 по 07.05.2026

### 4. Создан загрузчик HI2
- API отдаёт только 1 последнюю дату — отложен до накопления

### 5. Создан модуль технических признаков
- 10 признаков через TA-Lib (returns, volatility, volume_ratio, RSI, MACD, BB и др.)
- **Проверен на SBER:** 85 дней × 18 колонок

### 6. Зафиксирована инвентаризация проекта
- Что есть для фьючерсов (стратегии GLDRUBF, CNYRUBF, SBERF)
- Что есть для акций (новые модули)
- Чего не хватает (FutOI 60+ дней, HI2 история, брокер)

## 📁 НОВЫЕ ФАЙЛЫ
- `DataCollectors/m10_to_d1_converter.py`
- `My_Indicators/hi2_indicator.py`
- `My_Indicators/technical_features.py`

## 🔑 КЛЮЧЕВЫЕ ВЫВОДЫ
1. Super Candles через MOEXPy недоступны
2. HI2 отдаёт только 1 день — нужен сборщик
3. M10→D1 конвертер работает отлично
4. Технические признаки считаются корректно
5. FutOI копится стабильно — ML через 2 месяца
6. Фьючерсы — приоритет, акции — эксперимент
