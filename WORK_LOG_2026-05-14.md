## 14.05.2026 — ИТОГИ ДНЯ

### ✅ Сделано:
- **Сборщик свечей** (candles_collector.py) для фьючерсов и акций (M10, H1, D1). Добавлен в крон.
- **Дневной график** с SMA 20 и уровнями поддержки/сопротивления на странице FutOI
- **Единый торговый вердикт v3.0** — объединяет сигнал, тренд, уровни, настроение участников, риск и рекомендацию
- **Правило перегрева** с учётом юриков: LONG блокируется при fiz_buy_ratio > 80% И юрики продают
- **Отображение процентов:** показывается большинство (покупатели или продавцы)
- **Сигналы:** LONG, SHORT, NEUTRAL, WAIT_FOR_RETRACEMENT, WAIT_FOR_BOUNCE
- **Таблицы:** Торговый вердикт и Участники переделаны в компактные таблицы

### 🔺 Проектирование «Треугольника»:
- Спроектирована архитектура «Треугольника»: локальный VS Code + DeepSeek + Tampermonkey-скрипт
- Локальный VS Code остаётся основным редактором (Git Workflow сохранён)
- Серверный VS Code (:8080) — для мониторинга
- DeepSeek — «фабрика файлов» (выдаёт код целыми файлами)
- Tampermonkey — «курьер» (автоматизирует передачу кода между DeepSeek и локальным проектом)
- **Отложено:** реализация Tampermonkey-скрипта (вернёмся позже)

### 📋 Осталось сделать:
1. Уточнить тикеры RTS и BRENT (свечи не собираются)
2. Разобраться с двумя графиками на странице (M5 + D1)
3. Сборщик микроструктуры (Depth Chart + Delta)
4. Сборщик метаданных фьючерсов (экспирация, контанго)
5. Панель корреляций (RTS, BRENT, GAZP)
6. Интеграция HMM-моделей в дашборд
7. Исправление стоп-лоссов/тейк-профитов
8. Реализация Tampermonkey-скрипта для «Треугольника»
