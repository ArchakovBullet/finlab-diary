# Дневник разработки FinLabPy — 01.07.2026

## Тема: Отладка добавления тикеров в сканер фьючерсов, проблема с GD и срочными фьючерсами

### Реализовано

#### 1. DEBUG кнопки «Добавить в сканер»
- Кнопка нажимается, код внутри выполняется
- Свечи для GD скачиваются (GD_D1.parquet, GD_H1.parquet, GD_H4.parquet)
- Тикер GD добавляется в futoi_collector.py (TICKERS)

#### 2. Обнаружена корневая проблема
- Сканер берёт тикеры из *_futoi.parquet (функция load_futoi_data())
- GD_futoi.parquet не создаётся — сборщик FutOI не запускается автоматически
- При ручном запуске сборщика GD пропускается: api.get_futoi('GD') не возвращает данных

#### 3. Системная проблема с тикерами
- Вечные фьючерсы (GLDRUBF, SBERF, ...) — FutOI собирается
- Срочные фьючерсы (BR, SI, GD, ...) — FutOI не собирается
- Короткие коды мапятся на полные только для свечей, но не для FutOI
- _short_to_full содержит не все тикеры (нет GD, RI, MX, ED)

### Найденные проблемы
1. Нет единого маппинга короткий код -> полный код MOEX для всех систем
2. api.get_futoi() не принимает короткие коды срочных фьючерсов
3. Кнопка не запускает сборщик FutOI после добавления тикера
4. Тикер без FutOI не отображается в сканере вообще

### Следующие шаги
- [ ] Выяснить формат тикера для api.get_futoi(): короткий (BR) или полный (BRN6)
- [ ] Составить полный справочник тикеров для всех срочных фьючерсов
- [ ] Унифицировать маппинг: свечи, FutOI, HI2, Super Candles
- [ ] Доработать кнопку: автозапуск сборщика FutOI
- [ ] Сделать fallback-отображение тикеров без FutOI

### Статус
- GD не добавляется в сканер
- Срочные фьючерсы не имеют данных FutOI
- Требуется рефакторинг системы тикеров
