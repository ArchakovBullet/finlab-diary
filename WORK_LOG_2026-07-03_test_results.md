### Результаты теста api.get_futoi() (03.07.2026)

- MOEX API для FutOI принимает ТОЛЬКО короткие коды: BR, SI, GD, GLDRUBF
- Полные коды с экспирацией (BRN6, SIM6, GDU6) возвращают 0 записей
- _short_to_full используется только для свечей — это правильно
- GD успешно добавлен в сканер после ручного сбора FutOI

### Что починили

- Кнопка Добавить теперь запускает сборщик FutOI для нового тикера (subprocess.run)
- При ошибке показывает предупреждение с текстом ошибки
- GD_futoi.parquet создан, GD отображается в сканере
