## 03.08.2026 (понедельник)

### ✅ Сделано

**CR (фьючерс на юань):**
- Добавлен в tickers_config.json (futures + futures_boards)
- FutOI собран: 3000 строк, последняя 2026-08-03 12:20, fiz_buy_ratio = 85.8%
- Свечи: get_full_code('CR') -> CRU6, API отдаёт 394 свечи, но сборщик не сохраняет — нужен фикс

**Кнопка «Добавить» в дашборде (app_v2.py):**
- Исправлен баг: _full_code -> _new_ticker в проверке создания файла FutOI
- CR теперь корректно добавляется через кнопку

**HI2 (индекс концентрации):**
- MOEXPy.get_hi2() — добавлены параметры from_date/till_date
- hi2_collector.py — добавлен _resolve_full_code() для товарных фьючерсов
- Результат: BR, GD, SI, ED, MX собраны (ранее были 0)
- Акции: заморожены на 22.06.2026 — ждём исправления MOEX

**futoi_collector.py:**
- Добавлен _resolve_full_code() для срочных фьючерсов (GN -> GNU6, SA -> SAV6)
- GN, SA: API не отдаёт данные — ограничение MOEX

**candles_collector.py:**
- dt_from для новых тикеров уменьшен с 365 до 30 дней

**SuperCandles H4:**
- Добавлен доколлект за последние 3 дня (lookback_dates)
- Исправлена индентация и BOM-символ после правок
- Файл за 03.08 удалён — ждём проверки крона в 20:40 МСК

**Git:**
- Запушены 8 файлов с правками

### 🟡 Осталось

- CR свечи: сборщик не сохраняет файлы (разобраться с интервалом)
- SuperCandles H4: проверить автосбор после крона (20:40 МСК)
- HI2 для акций: ждать исправления MOEX (заморожен на 22.06)
- GN, SA FutOI: API MOEX не отдаёт данные
- VK-токены: отложено
- UM, Eu свечи: отстают (само догонится)
